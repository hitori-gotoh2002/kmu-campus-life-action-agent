# 🎓 KMU 캠퍼스 라이프 에이전트 — 사용설명서

> 처음 쓰는 분도 따라 할 수 있게 만든 설치·사용 가이드입니다.
> ① **커리어 추천**(공모전·대외활동·장학 등을 분야별로 추천) + ② **졸업 진단**(성적증명서로 부족 학점·수강 로드맵)을 한 웹에서 제공합니다.

## 구조 한눈에
```
노션      = 내 캘린더(시간표) + 포트폴리오   ← 이 둘만!
로컬 백엔드 = 프로필·추천이력·설정·졸업진단·API키  (data/agent.db, 자동 생성)
웹(Streamlit) = ⚙️설정 · 📋추천 리뷰 · 🎓졸업 진단
```

---

## 0. 준비물
- **Python 3.10+** (https://www.python.org, 설치 시 "Add to PATH" 체크)
- 계정: **OpenAI**, **Notion**, (선택)**Telegram**
- 약 15분

## 1. 설치
```bash
pip install -r requirements.txt
copy .env.example .env      # (mac/linux: cp)
```

## 2. Notion 준비 (캘린더 + 포트폴리오만)
1. **통합 만들기**: https://www.notion.so/my-integrations → New integration → **Internal Integration Secret**(`ntn_...`) 복사
2. `.env` 에 `NOTION_API_KEY=ntn_...` 입력 (또는 나중에 웹 ⚙️설정에서)
3. **빈 페이지 1개** 생성 → 페이지 `···` → **연결(Connections)** 에 통합 추가 → URL 끝 32자리 ID 복사
4. **캘린더 DB 자동 생성**:
   ```bash
   python setup_notion.py <32자리_페이지ID>
   ```
   → '내 캘린더' DB 생성 + `.env` 에 ID 기록. 노션에서 이 DB에 **시간표 입력** 후 **캘린더 뷰**(`+` → 캘린더) 추가
5. **포트폴리오 페이지** 작성(강점·관심사·프로젝트 자유서술) → 통합 연결 → ID 를 `.env`(`NOTION_PORTFOLIO_PAGE_ID`) 또는 웹 ⚙️설정에

> 노션에는 이 둘(캘린더·포트폴리오)만 있으면 됩니다. 나머지는 전부 로컬에 자동 저장됩니다.

## 3. 웹 실행 + 키 입력
```bash
streamlit run app.py        # http://localhost:8501
```
- **⚙️ 설정** 페이지에서 **OpenAI / Notion / Telegram 키**를 입력·저장 (개인별 비서로 동작, 로컬에만 저장)
- 같은 페이지에서 **분야별 자동수신**(매일/매주/수동/끄기 · 웹/텔레그램) 설정

## 4. 사용
- **🎓 졸업 진단**: 성적증명서 PDF 업로드 → (AI 추출 → 직접 확인·수정) → 부족 학점 + 수강 로드맵. **새로고침해도 유지**됩니다.
- **📋 추천 리뷰**: 분야별 추천을 보고 [✅노션에 추가](캘린더 등록) / [🗑️무시]
- **포트폴리오 반영**: `python analyze_portfolio.py` → 포트폴리오를 AI가 분석해 프로필 자동 갱신

### 추천 데이터 채우기
```bash
python main.py              # 공지 수집→분석→추천(백엔드 적재) ; 웹에서 새로고침
```

## 5. Telegram 자동수신 (선택)
1. @BotFather → `/newbot` → 토큰 받기 → ⚙️설정 또는 `.env`(`TELEGRAM_BOT_TOKEN`)
2. 봇에게 아무 메시지 1회 → `python get_chat_id.py` → `TELEGRAM_CHAT_ID` 입력
3. ⚙️설정에서 원하는 분야 **채널=텔레그램**, 주기=매일/매주
4. `python scheduler.py` → 정해진 시각에 추천을 **버튼과 함께** 텔레그램으로 발송 → **[✅노션에 추가]** 누르면 캘린더 등록

## 6. 자주 묻는 것
| 증상 | 해결 |
|------|------|
| 노션 "접근 불가" | 그 페이지에 통합 **연결(Connections)** 추가 확인 |
| chat_id 안 나옴 | 봇에게 메시지 먼저 보내고 `get_chat_id.py` 재실행 |
| 추천이 비어있음 | `python main.py` 먼저 실행 후 웹 새로고침 |
| 졸업 이수구분 이상 | 업로드 후 화면에서 직접 수정(AI 초안이라 부정확 가능) |
| 한글 깨짐(콘솔) | `set PYTHONIOENCODING=utf-8` 후 실행 |

## 7. 보안
- `.env` 와 `data/`(로컬 DB) 는 **공유/깃 업로드 금지** (`.gitignore` 에 제외됨)
- 키 노출 시 즉시 재발급: OpenAI(platform) · Notion(my-integrations) · Telegram(@BotFather `/revoke`)

## 8. 구성 (개발자용)
- `app.py` + `pages/` : 웹(⚙️설정/추천/졸업)
- `main.py`(추천 파이프라인) · `scheduler.py`(자동 발송)
- `modules/store.py` : 로컬 백엔드(SQLite) — 프로필·이력·설정·졸업데이터
- `modules/` : scraper·classifier·analyzer·validator·executor·history·profile·portfolio·preferences·digest·deadline·feedback·transcript·graduation·roadmap
- `config/requirements.json` : 입학년도별 졸업요건 · 전체 구조: `통합_설계도.pdf`
