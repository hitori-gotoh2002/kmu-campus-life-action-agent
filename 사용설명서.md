# KMU 캠퍼스 라이프 에이전트 사용설명서

이 문서는 처음 보는 사람이 웹을 켜고 사용하는 데 필요한 순서만 정리한 가이드입니다.

## 1. 이 웹이 하는 일

이 앱은 국민대 학생의 캠퍼스 생활과 커리어 활동을 관리합니다.

- 장학금, 공모전, 대외활동, 학사일정, 채용·인턴, 자격증 정보를 매일 수집
- 포트폴리오, 시간표, 실제 일정, 수강내역 기반 졸업 상태를 바탕으로 추천
- 추천을 웹에서 검토하고 필요하면 Notion 캘린더에 추가
- 텔레그램으로 받은 추천도 버튼 승인만 하면 Notion에 추가
- ON국민 수강내역 엑셀을 올리면 부족 학점과 졸업 위험 요소 확인

## 2. 전체 구조

```text
Streamlit 웹
├─ 설정: API 키, 시간표, 분야별 자동수신
├─ 내 맞춤 추천함: 추천 확인, 새로고침, 노션 추가, 무시
└─ 졸업 진단: 수강내역 엑셀 검증과 졸업요건 진단

저장소
├─ Supabase 원격 DB: 추천, 설정, 프로필, 시간표, 졸업 데이터
└─ SQLite 로컬 DB: Supabase를 쓰지 않을 때의 예비 저장소

외부 서비스
├─ Notion: 실제 일정 캘린더, 포트폴리오
├─ Telegram: 추천 발송, 승인 버튼
└─ GitHub Actions: PC가 꺼져 있어도 매일 08시 자동 실행
```

## 3. 설치와 실행

```powershell
pip install -r requirements.txt
copy .env.example .env
streamlit run app.py
```

웹 주소는 보통 <http://localhost:8501> 입니다.

## 4. 처음 설정하기

### 4.1 API 키 입력

웹 왼쪽 메뉴에서 `설정`을 엽니다.

입력할 값:

- `OPENAI_API_KEY`: 추천 요약과 분석에 사용
- `NOTION_API_KEY`: Notion 캘린더 등록에 사용
- `NOTION_CALENDAR_DB_ID`: 실제 일정이 들어갈 Notion 캘린더 DB
- `NOTION_PORTFOLIO_PAGE_ID`: 내 포트폴리오 페이지
- `TELEGRAM_BOT_TOKEN`: 텔레그램 발송용 봇
- `TELEGRAM_CHAT_ID`: 메시지를 받을 채팅 ID

Supabase 원격 DB를 쓰면 이 설정은 원격 DB에 저장될 수 있습니다. 개인 시연용이면 편하지만, 여러 사람이 같이 쓰는 운영 환경에서는 사람별 저장소 분리가 필요합니다.

### 4.2 Notion 준비

Notion은 두 가지만 사용합니다.

1. 실제 일정 캘린더
   - 회의, 모임, 면담, 회식처럼 진짜 일정만 넣습니다.
   - 수업 시간표는 넣지 않습니다.

2. 포트폴리오 페이지
   - 관심 직무, 프로젝트, 강점, 경험을 자유롭게 적습니다.
   - 앱이 이 내용을 읽어 추천 기준으로 사용합니다.

캘린더 DB가 없다면 아래 명령으로 자동 생성할 수 있습니다.

```powershell
python setup_notion.py <노션_페이지_ID>
```

### 4.3 시간표 입력

`설정` 화면의 `수업 시간표` 영역에서 시간표 PDF를 업로드합니다.

앱은 시간표를 분석해 주간 수업 시간을 계산합니다.  
자동 추출이 틀리면 표에서 직접 수정한 뒤 저장하면 됩니다.

중요한 원칙:

- Notion 캘린더: 실제 일정만 저장
- 웹 시간표: 반복 수업 시간만 저장

이렇게 분리해야 추천이 “진짜 여유 시간”을 계산할 수 있습니다.

### 4.4 분야별 자동수신

`설정` 화면 아래에서 분야별 주기와 채널을 정합니다.

| 주기 | 의미 |
|---|---|
| 매일 | 매일 자동 분석 대상 |
| 매주 | 정해진 요일에 자동 분석 대상 |
| 수동 | 자동 분석은 제외, 웹 새로고침 때만 분석 |
| 끄기 | 자동 분석과 수동 새로고침 모두 제외 |

| 채널 | 의미 |
|---|---|
| 웹 | 추천함에 누적 표시 |
| 텔레그램 | 자동 실행 시 1순위 추천을 텔레그램으로 발송 |

## 5. 추천 사용하기

`내 맞춤 추천함` 화면에서 분야 박스를 누르면 누적 추천이 보입니다.

각 추천에는 다음 정보가 표시됩니다.

- 제목과 분야
- 추천된 날짜
- 마감일
- 예상 필요 시간
- 내용 요약
- 왜 추천됐는지
- 신청/참여 전 확인할 점
- 원문 링크

버튼:

- `노션에 추가`: Notion 캘린더에 준비 일정과 상세 설명을 추가
- `무시`: 다시 보고 싶지 않은 추천으로 표시
- `새로고침`: 설정에서 꺼지지 않은 분야를 다시 수집하고, 신규/변경 공지만 분석

새로고침은 기존 추천을 지우지 않습니다. 같은 URL의 제목·본문·마감일·분야가 그대로면 기존 분석을 유지하고, 신규/변경 공지만 추가 분석합니다.

## 6. 텔레그램으로 받기

텔레그램 채널을 선택한 분야는 자동 실행 시 추천을 메시지로 받습니다.

동작 방식:

```text
08:00 GitHub Actions 실행
→ 정보 수집 및 추천 분석
→ Supabase에 저장
→ 텔레그램 수신 분야의 추천 발송
→ 사용자가 승인 버튼 클릭
→ 최대 5분 내 서버가 확인
→ Notion 캘린더에 추가
```

로컬 PC가 꺼져 있어도 GitHub Actions가 실행되면 텔레그램 발송은 가능합니다.

## 7. 졸업 진단 사용하기

`졸업 진단` 화면에서 ON국민 수강내역 엑셀 파일을 업로드합니다.

파일은 ON국민의 수강내역 또는 수강신청확인서에서 학기별로 내려받은 `.xls`/`.xlsx`를 사용합니다.
여러 학기 파일을 한 번에 올릴 수 있습니다.

앱이 추출하는 정보:

- 총 이수 학점
- 전공/교양 등 이수구분별 학점
- 부족 학점
- 졸업 위험 요소
- 이수 완료 과목
- 추천 로직에 반영할 미충족 요건과 보유 역량 신호

엑셀에는 성적과 GPA가 없기 때문에 졸업 평점 충족 여부는 화면에서 직접 선택합니다.
자동 매칭이 틀릴 수 있으므로, 업로드 후 포함 여부와 이수구분을 확인하고 필요하면 수정하세요.

AI 규정 해설·총평 토글을 켜면 요람 RAG 검색과 OpenAI를 사용해 규정 근거와 시나리오 총평을 추가합니다.

## 8. 서버 자동화

GitHub Actions가 켜져 있으면 로컬 PC와 무관하게 동작합니다.

- 매일 08:00 KST: 추천 수집/분석/저장/텔레그램 발송
- 5분마다: 텔레그램 승인 버튼 확인
- 저장 위치: Supabase 원격 DB

즉 PC가 꺼져 있어도 추천 데이터는 갱신됩니다.  
다만 `localhost:8501` 웹 화면은 로컬 PC가 켜져 있을 때만 접속할 수 있습니다. PC를 켜고 웹을 열면 Supabase의 최신 추천을 읽어옵니다.

## 9. 자주 생기는 문제

| 증상 | 확인할 것 |
|---|---|
| 웹이 안 열림 | `streamlit run app.py`가 실행 중인지 확인 |
| 추천이 비어 있음 | 설정에서 모든 분야가 `끄기`인지 확인, `새로고침` 실행 |
| Notion 추가 실패 | Notion 통합이 캘린더 DB에 연결됐는지 확인 |
| 텔레그램이 안 옴 | 해당 분야 채널이 `텔레그램`인지, GitHub Actions가 성공했는지 확인 |
| 승인했는데 Notion에 안 들어감 | 최대 5분 기다린 뒤 Actions 로그 확인 |
| 시간표가 이상함 | 설정 화면의 시간표 표를 직접 수정 후 저장 |
| 졸업 진단이 이상함 | 수강내역 엑셀 인식 결과를 화면에서 직접 수정 |

## 10. 보안

- `.env` 파일은 절대 GitHub에 올리지 않습니다.
- `SUPABASE_SERVICE_ROLE_KEY`는 강한 권한을 가진 서버 키입니다.
- 키를 채팅이나 문서에 노출했다면 Supabase에서 rotate 후 `.env`와 GitHub Secrets를 새 키로 바꾸세요.

## 11. 더 자세한 문서

- 프로젝트 구조: `README.md`
- 서버 자동화: `docs/server_automation.md`
- Supabase 테이블: `docs/supabase_schema.sql`
- 그림 기반 웹 사용 설계도: `웹_사용_설계도.pdf`
