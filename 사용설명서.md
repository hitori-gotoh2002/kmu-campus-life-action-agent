# 🎓 KMU 캠퍼스 라이프 에이전트 — 사용설명서

> 처음 쓰는 분도 따라 할 수 있게 만든 설치·사용 가이드입니다.
> 이 서비스는 ① **커리어 추천**(공모전·대외활동·장학 등을 내게 맞게 추천)과
> ② **졸업 진단**(성적증명서로 부족 학점·수강 로드맵 안내)을 한 웹에서 제공합니다.

---

## 0. 준비물

- **Python 3.10+** (설치: https://www.python.org → "Add to PATH" 체크)
- 계정 3개: **OpenAI**, **Notion**, **Telegram**
- 약 20분

---

## 1. 내려받기 & 설치

```bash
# 프로젝트 폴더에서
pip install -r requirements.txt
copy .env.example .env      # (mac/linux: cp .env.example .env)
```
→ 생긴 `.env` 파일에 아래에서 얻는 키들을 채웁니다. (`.env` 는 절대 공유·깃 업로드 금지)

---

## 2. OpenAI 키 (AI 분석용)

1. https://platform.openai.com → 로그인 → 왼쪽 **API keys**
2. **Create new secret key** → 복사 (`sk-...`)
3. `.env` 에 입력:
   ```
   OPENAI_API_KEY=sk-여기에붙여넣기
   ```

---

## 3. Notion 연동 (데이터 저장소)

### 3-1. 통합(integration) 만들기 → API 키
1. https://www.notion.so/my-integrations → **New integration**
2. 이름 입력(예: `campus-agent`) → 저장
3. **Internal Integration Secret** 복사 (`ntn_...`)
4. `.env` 에 입력: `NOTION_API_KEY=ntn_...`

### 3-2. 페이지 만들고 통합 "연결"
1. 노션에서 **빈 페이지** 생성 (예: "캠퍼스 에이전트")
2. 그 페이지 우측 상단 **`···` → 연결(Connections)** → 방금 만든 통합 선택
   - ⚠️ 이 "연결"을 안 하면 프로그램이 페이지에 접근할 수 없습니다.
3. 페이지 URL 끝의 **32자리 ID** 복사
   - 예) `notion.so/캠퍼스-에이전트-`**`37368380882880439d7ddb58b12280bc`**

### 3-3. 필요한 DB 자동 생성 (한 줄!)
```bash
python setup_notion.py <위에서_복사한_32자리ID> 2026
```
→ 프로필·이력·설정·캘린더·칸반·졸업요건·이수내역 **7개 DB를 자동 생성**하고
   `.env` 에 ID를 자동 기록합니다. (`2026` 자리는 본인 입학년도)

### 3-4. 포트폴리오 페이지 (선택, 추천 품질↑)
1. 노션에 본인 **포트폴리오 페이지** 작성(강점·관심사·프로젝트 등 자유 서술)
2. 그 페이지에도 **통합 연결**(3-2와 동일) + 32자리 ID 복사
3. `.env`: `NOTION_PORTFOLIO_PAGE_ID=...`
4. `python analyze_portfolio.py` → 포트폴리오를 AI가 분석해 '내 프로필' DB 자동 채움

---

## 4. Telegram 봇 (자동 추천서 받기, 선택)

1. 텔레그램에서 **@BotFather** 검색 → `/newbot`
2. 봇 이름 + username(반드시 `bot`으로 끝남) 입력 → **토큰** 받기 (`8xxxx:AAxx...`)
3. `.env`: `TELEGRAM_BOT_TOKEN=8xxxx:AAxx...`
4. 방금 만든 봇을 검색해 들어가 **아무 메시지나 1회 전송**
5. ```bash
   python get_chat_id.py
   ```
   → 나오는 `TELEGRAM_CHAT_ID=...` 를 `.env` 에 입력

---

## 5. 실행

```bash
# .env 에서 DEMO_MODE=false 로 바꾼 뒤
streamlit run app.py          # 웹 실행 → 브라우저 http://localhost:8501
```
- **🏠 홈**: 내 프로필 요약 + 두 기능 진입
- **📋 추천 리뷰**: 분야별 추천을 보고 [노션에 추가]/[무시]
- **🎓 졸업 진단**: 성적증명서 PDF 업로드 → 부족 학점 + 수강 로드맵

### 추천 데이터 채우기 (추천 리뷰가 비어 있으면)
```bash
python main.py                # 공지 수집→분석→이력에 '추천완료' 적재
# 또는 자동 발송:
python scheduler.py           # 매일 정해진 시각 텔레그램 추천서
```

### 처음엔 데모로 체험
`.env` 의 `DEMO_MODE=true` 면 키 없이 가짜 데이터로 전체 흐름을 볼 수 있습니다.

---

## 6. 자주 묻는 것

| 증상 | 해결 |
|------|------|
| 노션 "페이지 접근 불가" | 그 페이지에 통합 **연결(Connections)** 추가했는지 확인 |
| chat_id 안 나옴 | 봇에게 **메시지 먼저 전송** 후 `get_chat_id.py` 재실행 |
| 졸업 진단 이수구분 이상 | 업로드 후 화면에서 **직접 확인·수정**(AI 초안이라 부정확할 수 있음) |
| 졸업요건이 안 맞음 | `config/requirements.json` 또는 노션 '졸업요건' DB 값 수정 |
| 한글 깨짐(콘솔) | `set PYTHONIOENCODING=utf-8` 후 실행 |

---

## 7. 보안 (중요)

- **`.env` 는 절대 공유/깃 업로드 금지** (`.gitignore` 에 이미 제외됨)
- 키가 노출됐다면 즉시 재발급: OpenAI(platform) · Notion(my-integrations) · Telegram(@BotFather `/revoke`)

---

## 8. 구성 (개발자용)

- `app.py` + `pages/` : Streamlit 웹(홈/추천/졸업)
- `main.py` : 추천 파이프라인 / `scheduler.py` : 자동 발송
- `modules/` : scraper·classifier·analyzer·validator·executor·history·profile·portfolio·preferences·digest·deadline·feedback·transcript·graduation·roadmap
- `config/requirements.json` : 입학년도별 졸업요건
- 전체 구조: `통합_설계도.pdf`
